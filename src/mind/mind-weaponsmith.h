#pragma once

#include "system/angband.h"

bool item_tester_hook_melee_ammo(const player_type *player_ptr, object_type *o_ptr);
bool object_is_smith(const player_type *player_ptr, object_type *o_ptr);
